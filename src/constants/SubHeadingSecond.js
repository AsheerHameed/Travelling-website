import React from "react";
const SubHeadingSecond = ({ title, heading }) => (
  <div>
    <p className="p__font">{title}</p>
    <h1 className="head__font">{heading}</h1>
  </div>
);
export default SubHeadingSecond;
